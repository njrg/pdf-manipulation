# PDF Manipulation

Small python scripts utilizing pypdf to manipulate/edit pdf files.

Right now there are:

- `identify_rectangles.py`: Print coordinates of a rectangle annotation previously drawn with
  another application (eg. Okular)
- `add_hyperlink.py`: Add a hyperlink to a rectangle at specified coordinates
